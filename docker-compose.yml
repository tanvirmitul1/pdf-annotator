services:
  postgres:
    image: postgres:16-alpine
    container_name: pdf-annotator-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: pdf_annotator
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: pdf-annotator-redis
    restart: unless-stopped
    ports:
      - "16379:6379"
    volumes:
      - redis-data:/data

volumes:
  postgres-data:
  redis-data:
